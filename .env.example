# ─────────────────────────────────────────────────────────────────────────────
# Unstuck Internal Dashboard — Environment Variables
# ─────────────────────────────────────────────────────────────────────────────
# Copy this file, fill in the real values, and add them to Render.com under:
#   Dashboard → your service → Environment → Add Environment Variable
#
# DO NOT commit a filled-in copy of this file to Git.
# ─────────────────────────────────────────────────────────────────────────────


# ── Supabase (Mail Merge Tool — Sender Profiles) ──────────────────────────────
# Found in: Supabase dashboard → Project Settings → API

# Your project's API URL
SUPABASE_URL=https://xxxxxxxxxxxxxxxxxxxx.supabase.co

# Public anon key (safe for server-side use; NOT the service_role key)
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...


# ── Copy Bank — Slack Approval Notifications ──────────────────────────────────
# Required for the copy approval workflow (Request Approval → Slack notify → Approve & Publish)
#
# WEBHOOK URL
#   1. Go to https://api.slack.com/apps → Create New App → From scratch
#   2. Name: "Unstuck Copy Bank", select your workspace → Create App
#   3. Sidebar → Incoming Webhooks → Activate Incoming Webhooks ON
#   4. Add New Webhook to Workspace → select your #copy-approvals channel → Allow
#   5. Copy the Webhook URL below
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/XXX/YYY/ZZZ

# REVIEWER SLACK MEMBER IDs  (used to @mention reviewers in the notification)
#   Find each ID: click their Slack profile → ⋮ (three dots) → Copy member ID
#   Format: U012AB3CD
SLACK_OLLIE_ID=U0XXXXXXX
SLACK_CHRIS_ID=U0YYYYYYY
SLACK_LEO_ID=U0ZZZZZZZ


# ── Copy Bank — Google Docs Export ───────────────────────────────────────────
# Required only if you want the "Export as Google Doc" feature.
#
# Setup:
#   1. Go to https://console.cloud.google.com → New Project (or reuse one)
#   2. Enable APIs: "Google Docs API" and "Google Drive API"
#   3. Credentials → Create → OAuth 2.0 Client ID → Web application
#   4. Authorised redirect URIs: add  {APP_BASE_URL}/api/export/google/callback
#   5. Copy Client ID and Secret below
#
# APP_BASE_URL must be the base URL of your deployed app (no trailing slash)
GOOGLE_CLIENT_ID=xxxxxxxxxxxx.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-xxxxxxxxxxxxxxxx
APP_BASE_URL=https://your-app.onrender.com

# ── Outbound Pulse — campaign reporting connectors ───────────────────────────
# Powers the Outbound Pulse module (/outbound-pulse): pulls campaign performance
# from Smartlead and Meet Alfred into one normalized funnel per client.
#
# Run migrations/outbound_pulse_schema.sql in the Supabase SQL editor first.
# Neither key is required for the app to boot — with no key set, the hourly sync
# simply doesn't start and the connector shows as "not configured".

# SMARTLEAD (email)
#   Smartlead dashboard -> Settings -> API Key
SMARTLEAD_API_KEY=
# Override only if Smartlead moves the API host.
# SMARTLEAD_API_BASE=https://server.smartlead.ai/api/v1
# Pause between paged statistics requests, ms (rate-limit courtesy).
# SMARTLEAD_PACING_MS=300

# MEET ALFRED (LinkedIn)
#   API access is plan-dependent and not publicly documented — confirm the base
#   URL and endpoint paths against your account before setting these. Until then,
#   load LinkedIn data via the CSV import on the Outbound Pulse page, which maps
#   through the same normalization as the live connector.
MEET_ALFRED_API_KEY=
# MEET_ALFRED_API_BASE=https://api.meetalfred.com/v1
# MEET_ALFRED_CAMPAIGNS_PATH=/campaigns
# MEET_ALFRED_ACTIVITIES_PATH=/campaigns/{id}/activities

# TENANCY
#   Every Pulse table carries agency_id. There is one agency row; the app looks
#   it up by name and creates it if missing. Pin the id here only if the row is
#   ever recreated and you need queries to follow the new one.
# PULSE_AGENCY_NAME=Unstuck
# PULSE_AGENCY_ID=
# Pause between campaigns in a sync run, ms.
# PULSE_PACING_MS=500
